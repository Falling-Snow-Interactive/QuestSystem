using UnityEngine;

namespace Fsi.QuestSystem
{
    public enum QuestStatus
    {
        None = 0,
        Unassigned = 1,
        Active = 2,
        Complete = 3,
        Failed = 4,
        Abandoned = 5,
    }
}
